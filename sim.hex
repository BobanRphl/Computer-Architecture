#include <stdio.h>
#include "shell.h"

void fetch()     				/* This function fetches an instruction which is in hex format from the memory*/
{
	
}

void decode()   				/*This fuction decodes the fetched instruction and determines its type*/
{
    
}

void execute()					/*This function executes the instruction and updates the memory state*/
{

}
void process_instruction()
{
    /* execute one instruction here. You should use CURRENT_STATE and modify
     * values in NEXT_STATE. You can call mem_read_32() and mem_write_32() to
     * access memory. */
    fetch();
    decode();
    execute();

}
